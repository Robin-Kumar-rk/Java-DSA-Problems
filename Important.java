


public class Important{
    public static void main(String[] args) {
        /* 
          *(for each loop is used for iterating on collections of object & 
          we don't need to store that collection)
          
          *(for each loop is not executed when null is stored on every point in arr
          no statement will be executed)

          *(if we want to use a function of class & we don't want to 
            store object of that class then we can only create a object
            & direct use its function  example
            char ch = new StringBuilder("Robin").charAt(3);
            int a = new Scanner(System.in).nextInt;)
        
           * Integer.MIN_VALUE;   minimum value of Integer range
           * Integer.MAX_VALUE;   maximum value of Integer range
            
           * Math.min(a,b);
           * Math.sqrt(n);   returns double
           * Math.Pow(a,b);
           * Math.abs(a);   returns absolute value of a; a can be of any type
           * Math.ceil(a);  returns double x : x >= a and x will be integer
           * Math.floor(a); retunrs x : x <= a and x will be integer 

           * st1.toCharArray();  convert string to arr

           * Arrays.equals(arr1, arr2);  finds that arr1 is equal to arr2 or not 
           
           * int a = Character.getNumericValue(ch);  if char ch = '3' then a = 3
             
           * // inbuilt sort (ascending order) : For Arrays
             // import java.util.Arrays;
             // Arrays.sort(arr); tc O(nlogn)
             // Arrays.sort(arr,si,ei);
             // Arrays.fill();  // there are many variants of this function in java 
             // read on you own.Basically it is used to setting a value on all indexs of array

             // for decending order
             // import java.util.Collections;
             // Arrays.sort(arr,Collections.reverseOrder());
             // Arrays.sort(arr,si,ei,Collections.reverseOrder());
             // but reverseOrder() function works only on objects 
           * 
             
           * // inbuilt sort (ascending order) : For ArrayList
             // import java.util.Collections;
             // Collections.sort(list);

             // for decending order
             // Collections.sort(list, Collections.reverseOrder());
           *
            
            
           *  


         */
           
    }

    
}